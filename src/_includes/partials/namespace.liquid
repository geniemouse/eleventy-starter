<script>
(function () {
	"use strict";
	window.APP = {};
	APP.ENV = "{{ info.environment }}";
    APP.LANG = "{{ info.language }}";
	APP.LOCALE = "{{ info.locale }}";
	APP.VERSION = "{{ info.version }}";
	APP.URL = "{{ info.url }}";
})();
</script>
